/** Snapshot of run_pipeline.py + child CLI stdout; update pipelineTranscript.txt when CLIs change. */
import pipelineTranscript from "./pipelineTranscript.txt";

export const PIPELINE_TRANSCRIPT = pipelineTranscript as string;
